{% extends 'base.html.twig' %}

{% block title %}GopnikVideo{% endblock %}

{% block body %}
    <h1>GopnikVideo</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ gopnik_video.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ gopnik_video.title }}</td>
            </tr>
            <tr>
                <th>Text</th>
                <td>{{ gopnik_video.text }}</td>
            </tr>
            <tr>
                <th>VideoUrl</th>
                <td>{{ gopnik_video.videoUrl }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('gopnik_video_index') }}">back to list</a>

    <a href="{{ path('gopnik_video_edit', {'id': gopnik_video.id}) }}">edit</a>

    {{ include('gopnik_video/_delete_form.html.twig') }}
{% endblock %}
